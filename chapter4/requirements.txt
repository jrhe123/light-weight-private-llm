# Chapter 4 完整依赖列表
# 安装方式: pip install -r requirements.txt

# 核心深度学习框架
torch==2.2.2
numpy==1.26.4

# 通用工具
tqdm==4.66.1
matplotlib==3.7.1

# Hugging Face 生态系统
# 使用兼容的旧版本组合（避免 HybridCache 等新功能依赖）
transformers==4.38.2
peft==0.10.0
accelerate==0.30.0
datasets
